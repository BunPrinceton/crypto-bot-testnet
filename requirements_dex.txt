# Additional dependencies for DEX integration (Raydium, Pump.fun, and other Solana DEXs)
# Install with: pip install -r requirements_dex.txt

# Solana blockchain interaction
solders==0.21.0              # Solana Python SDK (Rust-based, faster)
solana==0.34.3               # Solana Python library
anchorpy==0.20.1             # Anchor framework Python client

# Alternative Solana client (lighter weight)
# solana-py==0.31.0          # Community Solana Python

# Base58 encoding (for Solana addresses)
base58==2.1.1

# Enhanced decimal precision for pricing calculations
# (stdlib Decimal is sufficient, but mpmath provides extras if needed)
# mpmath==1.3.0

# Already in requirements.txt but needed:
# requests>=2.31.0
# aiohttp>=3.9.0
# websockets>=12.0

# Optional: Web3 for cross-chain comparisons
# web3==6.11.3               # Ethereum/EVM chains

# Optional: Additional DEX aggregators
# jupiter-python-sdk==0.1.0  # Jupiter aggregator (Solana)

# Data analysis (already in main requirements.txt)
# pandas>=2.1.0
# numpy>=1.25.0

# Note: Some packages may require system dependencies:
# - solders requires Rust toolchain for building
# - On Ubuntu/Debian: apt-get install build-essential pkg-config libssl-dev
# - On MacOS: xcode-select --install
